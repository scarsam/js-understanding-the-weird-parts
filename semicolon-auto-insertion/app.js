function getPerson() {
  return { // return is looking for a space instead of a return so this work, if doing a return after return it will add a semicolon
    firstname: 'Sam'
  }
}
console.log(getComputedStyle());